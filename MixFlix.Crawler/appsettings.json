{
  "ConnectionStrings": {
    "Development": "Host=[host];Database=[database];Username=[user];Password=[password];Timeout=300;CommandTimeout=300;",
    "QA": "Host=[host];Database=[database];Username=[user];Password=[password];Timeout=300;CommandTimeout=300;",
    "Production": "Host=[host];Database=[database];Username=[user];Password=[password];Timeout=300;CommandTimeout=300;"
  },
  "Sync": {
    "Host": "",
    "Username": "",
    "Password": "",
    "LocalDir": "",
    "RemoteDir": "",
  }
}
